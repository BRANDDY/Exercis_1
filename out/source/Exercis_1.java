/* autogenerated by Processing revision 1276 on 2021-09-15 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class Exercis_1 extends PApplet {

float circleSize = 12;

 public void setup() {
    /* size commented out by preprocessor */; // P2D enables 2D GPU acceleration
    background(20);
    text("Clear Screen: L ",20,40);
    text("Rubber: RIght Click ",20,20);

}

 public void draw() {  
    if (mousePressed && (mouseButton == LEFT)) {
        ellipseMode(CENTER);
        fill(255);
        stroke(255);
        circle(mouseX, mouseY, circleSize);
    }
    if (mousePressed && (mouseButton == RIGHT)) {
        ellipseMode(CENTER);
        fill(20);
        stroke(20);
        square(mouseX, mouseY, 30);
    }
    if (keyPressed) {
        if (key == 'l' || key == 'L') {
            background(20);
            text("Clear Screen: L ",20,40);
            text("Rubber: RIght Click ",20,20);
        }
    } 
}


  public void settings() { size(800, 600, P2D); }

  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "Exercis_1" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
